Blender sources are saved in the state that is most convenient for editing, so models might NOT be optimized.
E.g. when exporting mc style models from blender sources do "Join",  "Remove doubles" and "Set Origin to Geometry" first.
(might be applicable to other models).